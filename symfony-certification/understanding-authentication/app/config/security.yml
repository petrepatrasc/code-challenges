security:
    providers:
        in_memory:
            memory: ~

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        main:
            pattern: ^/hello
            custom_oauth:
                check_path: oauth.login
            logout: true
            anonymous: true